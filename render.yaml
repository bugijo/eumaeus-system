databases:
  - name: Eumaeus-db
    plan: free

services:
  - type: web
    name: Eumaeus-backend-oregon
    env: node
    plan: free
    rootDir: backend
    buildCommand: |
      npm ci
      npx prisma generate
      npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: Eumaeus-db
          property: connectionString
      - key: NODE_ENV
        value: production
